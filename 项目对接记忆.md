# 项目对接记忆（Project Handoff Memory）

> **给下一个 Claude（或未来的我）**：这个文件是 ai-music-lab 项目的状态快照。
> 用户 qiaodaxian233 换对话时，把这个文件丢给你就能直接接着干。
> **第一件事**：读完这份文件再动代码。

---

## 👤 用户信息

- **GitHub**：[qiaodaxian233](https://github.com/qiaodaxian233)
- **仓库**：<https://github.com/qiaodaxian233/ai-music-lab>
- **默认分支**：`main`（注意：另一个项目 truth-dare-wheel 是 `master`，别混！）
- **可见性**：Private（自用）
- **本地路径**：用户自己机器，未约定（让他自己 git clone 到哪都行）
- **硬件**：本地 **12GB VRAM** 显卡
- **使用场景**：**纯本地自用**，不部署，不开放给别人，不需要队列/Web 后端/支付/账号
- **沟通语言**：中文。**不喜欢冗长解释**，喜欢直接动手 + 简短结论
- **更新方式偏好**：`git pull`，**不喜欢下载 zip 复制粘贴**

---

## 🎯 项目是什么

**ai-music-lab** —— 个人 AI 音乐生成工作台，是 [ACE-Step v1.5](https://github.com/ace-step/ACE-Step-1.5) 的包装层。

**本仓库不含模型代码**，只是围绕 ACE-Step 做的：
1. **一键安装脚本**（自动 clone ACE-Step、建 venv、装依赖）
2. **历史索引器**（每首生成的歌都登记，可搜可标可评分）
3. **自动后处理 watcher**（生成完自动归一化 + 转 MP3 + 加 ID3 tag）
4. **LoRA 数据准备 wizard**（参考歌曲分析 BPM/key + caption + 导出训练集）
5. **Song → DAW Export**（v0.3.0,6-stem 分离 + MIDI 转录 + Reaper 工程生成,**用户为摆脱 AI 标签做"真人创作"工作流**）
6. **风格 prompt 库** + **歌词模板**

**技术特点**：
- 纯 Python + Shell 脚本，没有 Web 后端
- 三个独立的 Gradio UI，分别监听 7860/7861/7862 端口
- 一个 watchdog daemon 跑后处理，CLI 启动
- 数据持久化：`outputs/.history-index.json` 单文件
- 没有数据库、没有账号、没有 Web 框架

---

## 📂 项目结构（v0.5.3 — 当前状态）

```
ai-music-lab/
├── README.md                       ← 启动说明
├── CHANGELOG.md                    ← 版本日志
├── 项目对接记忆.md                  ← 本文件
├── setup.sh                        ← 一键安装(clone ACE-Step + venv + 装依赖)
├── requirements.txt                ← Python 依赖
├── .gitattributes                  ← 行尾锁定 (v0.3.1)
├── .gitignore                      ← 排除权重/输出/数据集
│
├── start-ui.bat                    ← Windows: 启 ACE-Step 主 UI (调 ACE-Step 自带 start_gradio_ui.bat) (v0.4.0)
├── start-lab.bat                   ← Windows: 启统一控制台 (v0.4.0)
├── start-manager.bat               ← Windows: 启总控台 :7862 (v0.5.3)
│
├── ACE-Step-1.5/                   ← setup.sh 自动 clone,【不在 git 里】
│
├── lib/                            ← 核心 Python 库
│   ├── __init__.py
│   ├── history.py                  ← 历史索引 (#1, v0.2.0)
│   ├── postprocess.py              ← 后处理 (#2, v0.2.0)
│   ├── training_data.py            ← LoRA 数据 (#3, v0.2.0)
│   ├── stem_separation.py          ← Demucs 6-stem (#8, v0.3.0)
│   ├── audio_to_midi.py            ← Basic Pitch + 鼓 onset (#8, v0.3.0) + quick_transcribe (v0.5.1)
│   ├── reaper_project.py           ← Reaper .rpp 生成 (#8, v0.3.0)
│   ├── prompt_library.py           ← styles.json CRUD (#4, v0.5.0)
│   ├── lora_manager.py             ← LoRA 扫描+配置+试听 (#5, v0.5.0)
│   ├── batch_gen.py                ← CSV 批量生成 (#6, v0.5.0)
│   ├── cover_art.py                ← 封面 PIL+SDXL+ID3 (#9, v0.5.0)
│   ├── lrc_export.py               ← LRC 字幕 均分/Whisper (#10, v0.5.0)
│   ├── ace_step_api.py             ← ACE-Step pipeline 封装 (#6/#11/#12 共用, v0.5.0)
│   ├── export_bundle.py            ← 一键打包 zip (v0.5.2 #A)
│   ├── lora_merge.py               ← LoRA .safetensors 融合 (v0.5.2 #B)
│   ├── audio_highlight.py          ← chorus 检测 + 30s 剪 (v0.5.2 #C)
│   ├── pinyin_tools.py             ← 中文→拼音 (v0.5.2 #D)
│   └── process_manager.py          ← 启停 + 监控子进程 (v0.5.3, 跨平台 psutil)
│
├── scripts/                        ← 启动 / 命令行工具
│   ├── unified-ui.py               ← ★ 统一控制台 :7861 (v0.5.2, 12 个 Tab, 1902 行)
│   ├── lab-manager.py              ← 总控台 :7862 (v0.5.3, 启停监控其它服务, 333 行)
│   ├── launch-ui.sh                ← Linux: 启动 ACE-Step 主 UI :7860
│   ├── launch-history.sh           ← Linux: 启动历史浏览器 :7861 (单独)
│   ├── launch-lora-wizard.sh       ← Linux: 启动 LoRA wizard :7862 (单独)
│   ├── launch-daw-export.sh        ← Linux: 启动 Song → DAW Export :7863 (单独)
│   ├── launch-postprocess.sh       ← Linux: 启动后处理 watcher (单独)
│   ├── history-ui.py               ← 单独历史 UI (高级用户)
│   ├── lora-wizard.py              ← 单独 LoRA UI (高级用户)
│   ├── song-to-daw.py              ← 单独 DAW Export UI (高级用户)
│   ├── auto-postprocess.py         ← 后处理 daemon
│   ├── normalize.sh
│   └── batch-normalize.sh
│
├── prompts/
│   ├── styles.json
│   └── lyrics-templates.md
│
├── loras/
├── outputs/
│   ├── projects/                   ← DAW 工程 (v0.3.0)
│   ├── batches/                    ← 批量生成产物 (v0.5.0 #6)
│   ├── covers/                     ← 封面图 (v0.5.0 #9)
│   ├── lora-samples/               ← LoRA 自动试听产物 (v0.5.0 #5)
│   ├── midi/                       ← 一键 MIDI 产物 (v0.5.1)
│   ├── highlights/                 ← 30s 短版 (v0.5.2 #C)
│   └── bundles/                    ← 发行 zip (v0.5.2 #A)
└── datasets/
```

**未来可能加但目前没有**：
- `configs/`（README 提了占位，没文件，等需要再建）

---

## 🏗️ 架构关键点（不要打破）

### 1. 统一控制台 + ACE-Step 主 UI 分离（v0.4.0 起）

**v0.3.x 及之前**:4 个独立 Gradio 进程(7861/7862/7863 + watcher),用户要开 4 个窗口。

**v0.4.0 起**:`scripts/unified-ui.py` 合并 4 个 UI 为单进程,端口 7861,4 个 Tab,跨 Tab `gr.State` 联动。

**但 ACE-Step 主 UI 不能合并**(它是 ACE-Step 自己的 Gradio,运行在 7860 端口),用户在 ai-music-lab 控制台顶部点链接跳转。

**架构理由**:
- 用户体验:一个窗口一次操作,比开 4 个标签强太多
- 联动:历史里选一首歌 → DAW Tab 一键预填 / 后处理 Tab 一键跑
- 单端口少混淆
- 旧 scripts/{history-ui,lora-wizard,song-to-daw,auto-postprocess}.py **保留**,作为高级用户单独启动选项

| UI | 端口 | 启动 (Windows) | 启动 (Linux) | 用途 |
|---|---|---|---|---|
| ACE-Step 主 UI | 7860 | 双击 `start-ui.bat` | `cd ACE-Step-1.5 && bash start_gradio_ui.sh` | 生成 / LoRA 训练 |
| ★ **统一控制台** | 7861 | 双击 `start-lab.bat` | `python scripts/unified-ui.py` | 4 个 Tab: 历史/LoRA/DAW/后处理 |
| (单独)历史浏览器 | 7861 | — | `python scripts/history-ui.py` | 高级用户单独启动 |
| (单独)LoRA wizard | 7862 | — | `python scripts/lora-wizard.py` | 高级用户单独启动 |
| (单独)DAW Export | 7863 | — | `python scripts/song-to-daw.py` | 高级用户单独启动 |
| 后处理 watcher | 无 UI | (在统一控制台 🔄 Tab 启停) | `python scripts/auto-postprocess.py` | 监视 outputs/ 自动处理 |

**v0.4.0 之前文档说"不要合并 UI",这条已作废**。v0.4.0 合并了。
但 ACE-Step 主 UI 永远不能合并(它是 ACE-Step 自己的 Gradio)。

### 2. lib/ vs scripts/ 分层

- `lib/*.py` 是**纯函数库**，无 UI 依赖，可以 import 用
- `scripts/*.py` 是**入口程序**，含 Gradio UI 或 daemon 主循环
- 改业务逻辑 → 改 `lib/`，UI 不动；改 UI → 改 `scripts/<name>-ui.py`，lib 不动

### 3. 历史索引的设计

- 存储：`outputs/.history-index.json`，**整个目录 gitignore**（个人内容、可能含敏感歌词）
- ID 用 UUID（`uuid.uuid4().hex[:12]`），不是文件名 hash —— 文件名改了不会丢追踪
- **filename 是定位 key**：scan 时按 filename 查重，避免重复登记
- 字段：`filename / created_at / file_size_bytes / duration_seconds / prompt / lyrics / seed / params / lora / rating(0-5) / tags / notes / favorited / normalized / linked_normalized_id / linked_original_id`
- **`linked_normalized_id` / `linked_original_id`** 关联原始文件和归一化版本
- **`scan_and_update()`**：扫描新文件加入索引；**惰性**，只在用户点"刷新"或后处理 watcher 结束时调
- **`register()`**：未来如果搞自动化生成脚本（不通过 ACE-Step UI），可以从 Python 直接登记富元数据
- **sidecar 合并**：如果同名 `.json` 存在，自动合并到索引条目（为未来 ACE-Step 元数据预留）

### 4. 后处理 watcher 防循环（极重要）

watcher 监视 `outputs/`，处理完会**写新文件**到同一目录，触发 `on_created` 形成无限循环。

**防护**（`should_skip()` 在 `auto-postprocess.py`）：
- 跳过 `_normalized` 后缀文件
- 跳过 `_faded` 后缀文件
- 跳过 `.开头`的隐藏文件
- 维护 `processed: set[str]` 避免单次 session 重复

**改这里时务必同步**：postprocess.py 里产生新文件的后缀如果变了，watcher 的 skip 规则也要改。

### 5. 等文件稳定才处理（防止吃半成品）

`wait_for_stable(path, timeout=30)` —— 每秒检查文件大小，连续两秒不变才认为写完。

理由：ACE-Step 写 WAV 是流式的，文件出现时还在写。立刻 ffmpeg 处理会拿到不完整数据。

### 6. ID3 tag 不能写 WAV

WAV 不支持标准 ID3 tag。所以 `tag_audio()` 对 `.wav` 直接返回 False。

正确流程：归一化 WAV → 转 MP3 → 给 MP3 打 tag。`process_one()` 已经按这个顺序。

### 7. LoRA 数据 wizard 的两阶段

wizard 不是"一键完成"，是**两阶段**：
1. **分析阶段**：`prepare_dataset()` 扫描 raw/、重采样、librosa 分析、写初版 `metadata.csv`
2. **编辑阶段**：用户在 UI 表格里改 caption，点保存触发 `update_captions()` 回写 csv

**别合并成一步** —— caption 是 LoRA 训练的核心，必须让用户精心编辑。AI 猜的只是起点。

### 8. librosa 是可选的

`lib/training_data.py` 用 `_check_librosa()` 软依赖。没装也能 import 文件，只是 `analyze_audio()` 会返回 `{"error": "librosa 未安装"}`。

理由：librosa 拖很多依赖（numba、scipy），万一用户机器装不上还能用其它功能。

但 `requirements.txt` 里默认装。

---

## ⚠️ 已知避坑

### A. Python 版本必须 ≥3.11.12

- Ubuntu 自带的 `3.11.0rc1` 是**预发布版**，配 vLLM 后端会段错误（参考 ACE-Step 官方 INSTALL.md）
- `setup.sh` 会检查 Python 版本，<3.11.12 时给警告并要求用户确认
- 升级方法（Ubuntu）：deadsnakes PPA 装 3.11.x

### B. 默认分支是 `main`（不是 `master`）

用户的另一个项目 truth-dare-wheel 是 `master`。这个项目是 `main`。PAT push 时别推错分支。

### C. PAT 撤销验证

每次 push 完，**用户原话**：去 GitHub settings 撤销 PAT，下次给新的。  
**别留 PAT 在沙箱**：所有 `git clone` / `git push` 之后必须 `rm -rf /tmp/push_work`。  
**所有命令输出都用 sed 过滤** PAT 字符串：  
```bash
2>&1 | sed 's/[A-Za-z0-9_]\{30,\}/<REDACTED>/g'
```

### D. ACE-Step LoRA 训练数据格式不确定

我（v0.2.0 的 Claude）**没有亲自跑过 ACE-Step LoRA 训练**，只是按通用的 `metadata.csv` 格式（`filename, caption, bpm, key, duration`）输出。

ACE-Step 的 LoRA Training 标签可能期望略不同的 schema（比如要求 `text` 列而不是 `caption`）。

**下次有用户实际跑训练时，如果报错**：去 `ACE-Step-1.5/` 看官方 LoRA 训练教程（`docs/en/LORA_TRAINING.md` 或 Gradio UI 的提示），按它的格式改 `lib/training_data.py` 的 `prepare_dataset()` 里 CSV 写入部分。**改一处即可**。

### E. Gradio Dataframe 的 select 事件

`history-ui.py` 用 `gr.Dataframe` + `select` 事件实现"点行加载详情"。Gradio 不同版本 `SelectData.index` 类型不一样（有时是 int，有时是 [row, col]）。代码里做了兼容：
```python
row_idx = evt.index[0] if isinstance(evt.index, list) else evt.index
```
Gradio 升级后如果选行没反应，先看这个。

### F. librosa 第一次启动很慢

numba JIT 编译，首次分析一首歌可能要等 30-60 秒。第二次以后秒开。

**不要误报为 bug**。UI 上没显示进度条（Gradio 默认有 spinner）。

### G. `outputs/.history-index.json` 不要手动改

JSON 写盘是**全量重写**，不是增量 patch。用户在 UI 外手动改了 JSON 然后又在 UI 里点保存，他的修改会丢。

要批量改数据建议写脚本调 `lib.history` 的 API。

---

## 🔧 UI 启动与端口（速查 — v0.4.0）

**首选方式(Windows)**:
```cmd
:: 双击根目录
start-ui.bat            :: ACE-Step 主 UI    → :7860
start-lab.bat           :: 统一控制台 (4 Tab) → :7861
```

**首选方式(Linux/Mac)**:
```bash
cd ACE-Step-1.5 && bash start_gradio_ui.sh    # :7860 (ACE-Step 自带启动器)
python scripts/unified-ui.py                  # :7861 (统一控制台)
```

**单独工具(高级用户,基本用不上)**:
```bash
python scripts/history-ui.py        # :7861
python scripts/lora-wizard.py       # :7862
python scripts/song-to-daw.py       # :7863
python scripts/auto-postprocess.py  # 无 UI watcher
```

---

## 🎓 LoRA 训练完整流程（用户最关心）

```
[LoRA wizard UI :7862]
  ├─ 1. 输入项目名 → 点"➕ 创建" → 自动建 datasets/<项目>/raw/
  ├─ 2. 用户手动把 5-20 首参考 MP3/WAV 拖进 raw/
  ├─ 3. 选项目 → 点"🔍 分析并准备训练集"
  │     → librosa 分析每首 BPM/key
  │     → 文件名猜风格生成 caption 建议
  │     → 重采样到 44.1kHz stereo,写到 datasets/<项目>/audio/
  │     → 写 metadata.csv (filename, caption, bpm, key, duration)
  ├─ 4. 在表格里编辑每首的 caption (LoRA 训练核心)
  └─ 5. 点"💾 保存 captions" → 写回 metadata.csv

[ACE-Step 主 UI :7860 → "LoRA Training" 标签]
  ├─ 6. 数据集路径填: datasets/<项目>/
  ├─ 7. 配置训练参数 (12GB VRAM 勾 gradient checkpointing)
  └─ 8. 一键训练 → 训完 LoRA 自动可用于生成
```

---

## 🔌 lib/ 模块 API 清单

### `lib/history.py`

```python
load_index() -> dict
save_index(index: dict) -> None
scan_and_update(outputs_dir=OUTPUTS_DIR) -> (added: int, total: int)
register(filename: str, **metadata) -> Optional[str]  # 返回 fid
update_entry(fid: str, **fields) -> bool
delete_entry(fid: str, delete_file=False) -> bool
search(query="", favorites_only=False, min_rating=0) -> list[(fid, entry)]
get_audio_duration(path: Path) -> Optional[float]
```

### `lib/postprocess.py`

```python
normalize_loudness(input_path, output_path=None, target_lufs=-14.0, true_peak=-1.5, lra=11.0) -> Path
tag_audio(path, title="", artist="AI", album="ai-music-lab", comment="", genre="") -> bool
wav_to_mp3(wav_path, mp3_path=None, bitrate="320k") -> Path
fade_in_out(input_path, output_path=None, fade_in_sec=0.5, fade_out_sec=2.0) -> Path
process_one(input_path, *, normalize=True, fade=False, convert_mp3=True,
            title="", artist="AI", album="ai-music-lab",
            delete_intermediates=True) -> dict  # report
```

### `lib/training_data.py`

```python
analyze_audio(path: Path) -> dict  # {filename, duration, sample_rate, bpm, key, error}
suggest_caption(info: dict, source_filename="") -> str
prepare_dataset(source_dir, output_dir, target_sr=44100,
                target_format="wav", target_channels=2) -> dict  # report
update_captions(metadata_csv: Path, captions: dict[str, str]) -> int  # 返回更新行数
```

---

## 🔄 PAT 推送工作流（重要）

每次用户给 PAT 让你推 GitHub 时，**严格按这个流程**：

```bash
# 1. 临时目录 clone (PAT 只在 URL 里,不进 git config)
mkdir -p /tmp/push_work && cd /tmp/push_work
PAT="github_pat_xxx"
git clone "https://x-access-token:${PAT}@github.com/qiaodaxian233/ai-music-lab.git" 2>&1 | sed 's/[A-Za-z0-9_]\{30,\}/<REDACTED>/g'

# 2. 拷贝改动的文件
cd ai-music-lab
cp /home/claude/ai-music-lab/<改过的文件> ./<对应路径>
# ⚠ outputs/ 和 datasets/ 永远不要碰

# 3. 配置 git
git config user.name "qiaodaxian233"
git config user.email "qiaodaxian233@users.noreply.github.com"

# 4. commit
git add -A
git commit -m "..." 2>&1 | sed 's/[A-Za-z0-9_]\{30,\}/<REDACTED>/g'

# 5. push (注意分支是 main,不是 master!)
git push "https://x-access-token:${PAT}@github.com/qiaodaxian233/ai-music-lab.git" main 2>&1 | sed 's/[A-Za-z0-9_]\{30,\}/<REDACTED>/g'

# 6. 清理
rm -rf /tmp/push_work
```

**所有命令输出都用 `sed 's/[A-Za-z0-9_]\{30,\}/<REDACTED>/g'` 过滤**，确保 PAT 不出现在响应里。

### 撤销验证流程（推送完一定要做）

```bash
# 1. 让用户去 https://github.com/settings/tokens 撤销
# 2. 用一个写操作验证 token 真的死了
mkdir -p /tmp/verify && cd /tmp/verify
PAT="..."
git clone --depth 1 "https://x-access-token:${PAT}@github.com/qiaodaxian233/ai-music-lab.git" 2>&1 | sed 's/[A-Za-z0-9_]\{30,\}/<REDACTED>/g'
cd ai-music-lab
git config user.email "test@test.com"
git config user.name "test"
git tag _revocation_test_DELETE_ME -m "test"
git push "https://x-access-token:${PAT}@github.com/qiaodaxian233/ai-music-lab.git" _revocation_test_DELETE_ME 2>&1 | sed 's/[A-Za-z0-9_]\{30,\}/<REDACTED>/g'
# 期望看到: "Invalid username or token" 或 "fatal: Authentication failed"
rm -rf /tmp/verify
```

---

## 📜 版本历史

### v0.5.3 — 总控台 (端口 7862,启停 + 监控 3 个服务) (2026-05-15)

**用户原话**: "写一个Py 带UI 的管理器 可以运行所有功能"

理解: 不是再做一个 12 Tab 的功能 UI(那是 unified-ui.py 的事),而是更高一层的**运维总控**: 启停 + 监控 ACE-Step / 统一控制台 / watcher 三个常驻进程。

**新文件**:
- `scripts/lab-manager.py` (333 行,端口 7862,Gradio UI)
- `lib/process_manager.py` (405 行,跨平台子进程管理)
- `start-manager.bat`

**修改**:
- `requirements.txt` 加 `psutil>=5.9`
- `README.md` 启动表加总控台,**推荐新用户只双击 start-manager.bat 一个**
- `lib/__init__.py` / `CHANGELOG.md` / 本文件

**总控台 UI 区块** (单页):

| 区块 | 内容 |
|---|---|
| 🚦 服务控制 | ACE-Step / 统一控制台 / watcher 各一卡,启动/停止/重启/打开浏览器 |
| 📊 系统状态 | GPU 显存条 + util% (nvidia-smi)、磁盘、outputs/ 体积、LoRA 数 |
| 📂 最近输出 | outputs/ 最近 10 个改动 |
| 📜 日志 | 选服务看最近 200 行 (runtime/<key>.log) |
| 🔄 自动刷新 | gr.Timer 3s |

**关键设计**(下一个 Claude 注意):

- **不是又造一个功能 UI**: 跟 unified-ui.py 严格分工。功能去 7861, 运维去 7862。两个进程独立,7862 挂了不影响生成。
- **进程树 kill**: Windows 上 `cmd /c start_gradio_ui.bat` 启动后, PID 是 cmd.exe, 真正 Python 是孙子进程。简单 kill PID 不干净。用 `psutil.Process.children(recursive=True)` 拿整棵树, 先 terminate 3s 后 kill。
- **PID 文件 + 端口监听双判定**: 状态用 `pid_alive(read_pid()) OR port_listening(port)` 综合判断, 即使 PID 文件丢了/孤儿,端口还在 listen 就算"运行中"。这让总控台能感知到通过其它方式启动的服务。
- **隐藏 cmd 窗口**: Windows `CREATE_NO_WINDOW = 0x08000000`,服务后台跑,日志重定向到 `runtime/<key>.log`,在 UI 看。这跟现有 `start-ui.bat` 模式(弹 cmd 窗口看日志)不冲突——用户既可以双击 .bat 看窗口,也可以用总控台后台跑。两种模式都支持。
- **runtime/ 目录**: 跑起来才生成 `<key>.pid` 和 `<key>.log`,已加 .gitignore? 没有, 但内容是 runtime 临时, 不会进 git (因为 .pid/.log 不重要,目录里没追踪的文件)。如果干净要 push 可加 `runtime/` 到 .gitignore。

---

### v0.5.2 — 4 个高性价比补充功能 (2026-05-15)

**用户原话**: "做推荐的四个"(从我列的高/中/大性价比清单里挑了 4 个 🟢 高)

**4 个功能 + 入口位置**:

| 编号 | 功能 | 入口 |
|---|---|---|
| #A | 一键打包 zip (wav/mp3/cover/lrc/mid/metadata) | 新 Tab「📦 发行打包」(左) |
| #C | 自动剪短版 (chorus 检测 + 30s + 淡入淡出) | 新 Tab「📦 发行打包」(右,跟 #A 同 Tab) |
| #B | LoRA .safetensors 权重融合 | 「🎓 LoRA 库」Tab 底部子区 |
| #D | 中文→拼音 | 「📝 Prompt 工作室」Tab 底部子区 |

**改动文件**:

- 新增 4 个 lib 模块:
  - `lib/export_bundle.py` — `build_bundle()`,串联打包,缺什么自动生成什么
  - `lib/lora_merge.py` — `merge_two(a, b, wa, wb)` + `auto_generate_config()`,float32 加再转回原 dtype,形状不匹配的张量(常见于 alpha 标量)只取 A 不报错
  - `lib/audio_highlight.py` — `make_highlight()`,chroma + recurrence_matrix 算每帧重复度,滑窗找 target_seconds 最大窗口
  - `lib/pinyin_tools.py` — `to_pinyin / to_pinyin_inline / mixed_format`,保留 `[Verse]` 章节标签和标点
- 修改 `scripts/unified-ui.py` (1607→1902 行) — 新 Tab 12 + 在 Prompt 工作室 / LoRA 库 两个旧 Tab 底部分别加子工具
- 修改 `lib/__init__.py` / `requirements.txt` (加 pypinyin + safetensors) / `README.md`

**关键设计**(下一个 Claude 注意):

- **打包"缺啥补啥"策略**: `build_bundle` 不强制要求所有衍生物已存在,如果选了"含封面"但目录没,会现调 `cover_art.make_cover_geometric` 生成;lrc 类似(从历史索引拿歌词现转);midi 类似(`quick_transcribe`)。这让用户**只点一个按钮**就能发歌。
- **LoRA merge 共同 key 才融合**: ACE-Step LoRA 不同训练配置可能产生不同的 key 集合。`merge_two` 走"共同 key 加权 + 只一边的直接复制",这是最稳的策略。形状不匹配的(通常是 alpha 标量)记录到 `shape_mismatches` 报告但不致命。
- **chorus 检测算法**: 算 chroma 自相似矩阵 → 每帧"重复度"(跟其他帧的余弦相似度之和)→ target_seconds 滑窗最大值。**重复度高的位置 = 副歌**(因为副歌跟自己的多次出现相似度高)。沙箱测试 60s 模拟歌(chorus 在 20-40s 和 50-60s),检测到 24-54s 落在 chorus 区。
- **拼音保留标签和标点**: ACE-Step 输入歌词时,章节标签 `[Verse 1]` `[Chorus]` 是 ACE-Step 的语法,**不能**转。`SECTION_TAG_RE` 检测整行是标签就原样输出。

---

### v0.5.1 — 一键转 MIDI Tab (2026-05-14)

**用户原话**: "撤销了 然后你现在看看还有那个没弄 一键转MIDI 做了吗"
+ "加,但功能再扩点(同时出 MIDI + BPM + 段落 marker)"

**背景**: v0.5.0 完成 12 项功能后,用户发现 MIDI 转录虽然在 v0.3.0 做过(`lib/audio_to_midi.py` + Song→DAW Export Tab),但**必须经过 Demucs stem 分离**(5GB 模型 + 几分钟),没有独立"一键"入口。v0.5.1 补这个缺口。

**改动文件**:

- 修改 `lib/audio_to_midi.py` (186→326 行) — 新增:
  - `quick_transcribe(audio_path, *, mode, with_bpm, with_markers, embed_meta)`:高级一键 API,串好 BPM + marker + melodic + drums,支持 melodic/drums/both 三模式
  - `_inject_meta_into_midi()`:把 BPM 重写到 MIDI tempo,把段落 marker 写成 pretty_midi lyric 事件(Reaper / Ableton / Cubase 都认)
- 修改 `scripts/unified-ui.py` (1438→1607 行) — 加:
  - Tab 11 「🎼 一键 MIDI」
  - `qm_` 系列辅助函数(`qm_run` / `qm_run_upload` / `qm_song_choices`)
  - 支持 outputs/ 选歌 OR 直接拖音频上传

**关键设计**:

- **跳过 Demucs**:整曲直接送 Basic Pitch 抓主旋律,启动→出 MIDI 只要几秒(首次下 Basic Pitch ~150MB 除外)。比走 Song→DAW Export 路径快约 10×。
- **优雅降级**:Basic Pitch 没装时 `melodic` 失败,但 `drums` / `bpm` / `markers` 三项不依赖它,仍能出 drums.mid + bpm.txt + markers.txt(沙箱实测验证)。
- **跟 Song→DAW Export 共用 `lib/audio_to_midi.py`**,不重复代码,两个入口共享所有改进。
- **嵌入元数据策略**:pretty_midi 没有简单 API 改已存在 MIDI 的 initial_tempo,所以 `_inject_meta_into_midi()` 走"新建 PrettyMIDI(initial_tempo=新BPM) + 把旧 instruments 搬过来 + 加 lyrics 当 marker"的绕法。沙箱实测出来的 MIDI BPM 正确、3 个 lyric 事件正确。

**输出目录约定**: `outputs/midi/<歌的 stem 名>/` 下放 `melodic.mid` / `drums.mid` / `bpm.txt` / `markers.txt`。

---

### v0.5.0 — 8 大功能补全 (2026-05-14)

**用户原话**: "4 5 6 7 9 10 1 12" (= 4/5/6/7/9/10/**11**/12, "1" 是 "11" 的笔误)

一次性把 12 项规划里剩下的 8 项全做完。**12/12 完成**。统一控制台从 4 Tab 扩到 **10 Tab**,unified-ui.py 从 691 行扩到 1438 行。

**改动文件**:
- 新增 6 个 lib 模块:
  - `lib/prompt_library.py` — `styles.json` CRUD + tag 组合
  - `lib/lora_manager.py` — loras/ 扫描 + 配置 + 试听
  - `lib/batch_gen.py` — CSV 批量,带 dry_run / progress_callback / stop_flag
  - `lib/cover_art.py` — PIL 几何封面 + SDXL 可选 + MP3 ID3 嵌入
  - `lib/lrc_export.py` — LRC 字幕,均分 / Whisper 可选
  - `lib/ace_step_api.py` — ACE-Step pipeline 封装,#6/#11/#12 共用
- 修改 `scripts/unified-ui.py` — 加 6 个新 Tab (`ps_/lm_/bg_/ab_/cv_/rv_` 前缀)
- 修改 `lib/__init__.py` — 文档加新模块
- 修改 `requirements.txt` — `pillow>=10.0` 必装,diffusers / openai-whisper 注释为可选
- 修改 `README.md` — 主表扩到 10 Tab
- 修改 `CHANGELOG.md` — v0.5.0 条目

**6 个新 Tab**:

| Tab | 功能 | 关键依赖 |
|---|---|---|
| 📝 Prompt 工作室 | #4 预设 CRUD + tag 积木 | 仅 styles.json,无 GPU |
| 🎓 LoRA 库 | #5 LoRA 配置 + N seed 试听 | 试听需 ACE-Step |
| 🚀 批量生成 | #6 CSV 挂机跑 | ACE-Step,有 dry_run |
| ⚖ A/B 对比 | #7 两首并排听 | 仅历史索引 |
| 🎨 封面 & 字幕 | #9 + #10 | PIL 默认,SDXL/Whisper 可选 |
| 🔁 续写 / 翻唱 | #11 + #12 | ACE-Step audio2audio |

**关键设计**(下一个 Claude 注意):
- `ace_step_api.py` 是 6/11/12 共用入口,**懒加载 + 单例**(`_pipeline` 模块级 + `_pipeline_lock`),避免每次调用重新加载 12GB 模型。
- 该文件的 `__call__` 参数签名是**基于 ACE-Step v1.5 主线推断的**,首次实跑可能需要按报错改 (`manual_seeds` / `save_path` / `task` 字符串 / `repaint_*` / `ref_audio_strength` 这几个最可能)。改一处所有 6/11/12 都能跑。
- 沉重依赖(SDXL = diffusers + 6GB 模型,Whisper = 1-2GB 模型)做成可选,默认走零依赖 fallback(PIL 几何 / 均分对齐)。
- 多 LoRA 混合的字符串语法 `"path1:w1,path2:w2"` 是猜的,看实测。

---

### v0.4.0 — 统一控制台（2026-05-12）

**用户原话**: "这几个 IP 是不是重复啊 功能在哪里呢" + "这几个现在是联动吗?写一个 PY 程序 UI"

把 v0.2.0 / v0.3.0 攒下来的 4 个独立 Gradio UI 合并到单一程序。

**改动文件**:
- 新增 `scripts/unified-ui.py` (690 行) — 4 Tab 统一控制台 :7861
- 新增 `start-lab.bat` — Windows 启动统一控制台
- 修改 `start-ui.bat` — 改成调用 ACE-Step 自带 `start_gradio_ui.bat` (v0.3.1 写错了入口)
- 删除 `start-history.bat` / `start-lora-wizard.bat` / `start-daw-export.bat` / `start-postprocess.bat` (功能并入统一控制台)
- 修改 `README.md` — 主推 2-bat 方案
- 修改 `CHANGELOG.md` / `项目对接记忆.md` 同步

**统一控制台 Tab 设计**:
- 📚 历史浏览器(来自 history-ui.py)
- 🎓 LoRA 训练数据(来自 lora-wizard.py)
- 🎹 Song → DAW Export(来自 song-to-daw.py)
- 🔄 后处理控制(新增,包含 watcher 启停 + 手动跑)

**跨 Tab 联动**(关键):
- 顶层 `gr.State("")` 名为 `selected_song`
- 历史 Tab `h_table.select` 回调输出末位写入 `selected_song`
- DAW Tab 有按钮 `⬅ 用历史 Tab 选中的歌` 调 `d_use_history_selection`,从 `selected_song` 读取填到 dropdown
- 后处理 Tab 有按钮 `🔧 立即后处理选中的歌` 调 `p_run_once`,直接调用 `postprocess.process_one`

**watcher 子进程管理**:
- `_watcher_proc: subprocess.Popen | None` 模块级变量
- 启动:`subprocess.Popen([sys.executable, scripts/auto-postprocess.py])`
- 停止:`terminate() + wait(5) + kill()` 兜底
- 状态:`poll() is None` 判活

**旧的 4 个 scripts/*-ui.py 保留**:
- 用户/我可能想单独启动其中一个(更轻量)
- 高级用户场景

**关键决策**:
- 不删除单 UI 脚本(向后兼容、单独启动)
- ACE-Step 主 UI 永远不能合并(它在另一个 Python 进程,我们不控)
- 顶部 Markdown 链接 `[ACE-Step 主 UI](http://localhost:7860)` 提示用户切换

### v0.3.1 — Windows 启动器 + 行尾规范（2026-05-12）

修复 Windows 用户首次跑 `bash scripts/launch-ui.sh` 报 `$'\r': command not found` 的问题。

**改动文件**：
- 新增 5 个 `.bat` 启动器（`start-ui.bat`、`start-history.bat`、`start-lora-wizard.bat`、`start-daw-export.bat`、`start-postprocess.bat`）
- 新增 `.gitattributes` 锁定行尾（`.sh` 强制 LF，`.bat` 强制 CRLF）
- README 加 Windows 启动一节
- CHANGELOG 记录

**关键技术点**：
- `.bat` 必须 CRLF 否则 Windows cmd.exe 有时执行有问题
- `.sh` 必须 LF 否则 Git Bash 报 `$'\r'` 错误
- 所有 `.bat` 首行加 `chcp 65001 >nul` 切到 UTF-8，让中文不乱码
- `start-ui.bat` 带了 12GB VRAM 优化参数（`--bf16 --torch_compile --cpu_offload --overlapped_decode`），用户原版 `start_ui.bat` 没带这些参数会 OOM

**用户已经 pull 过 CRLF 文件的修复办法**：
```bash
sed -i 's/\r$//' setup.sh scripts/*.sh
```

### v0.3.0 — Song → Reaper 工程导出（2026-05-12）

commit 在 main 分支，本对话产出。

**核心动机**：用户的最终目标不是"让 AI 歌好听"，而是 **"摆脱 AI 标签 + 拿到工程文件作为创作证据"**。

**实现路径**：把 AI 歌拆成 stem 和 MIDI，自动生成 Reaper 工程，配合用户的电容麦录人声 → 工程里有实质性人类创作（录音 take、剪辑决策、混音），从而构成真实的创作证据。

**改动文件**：
- 新增 `lib/stem_separation.py`（Demucs htdemucs_6s 包装）
- 新增 `lib/audio_to_midi.py`（Basic Pitch 旋律转录 + librosa onset 鼓检测 + BPM + 段落 markers）
- 新增 `lib/reaper_project.py`（`.rpp` 文件生成,**MIDI events 直接嵌入到 .rpp 里**，用户打开就能播）
- 新增 `scripts/song-to-daw.py`（Gradio UI 端口 7863）
- 新增 `scripts/launch-daw-export.sh`
- 修改 `requirements.txt`（加 demucs / basic-pitch / pretty_midi）
- 修改 `README.md`、`CHANGELOG.md`、`项目对接记忆.md`

**关键架构决策**（不要轻易改）：
- **不预装 plugin chain 到 .rpp**：Reaper plugin 参数格式是 version-specific base64 blob，跨版本不稳。让用户自己加 ReaEQ/ReaComp（30 秒）
- **MIDI events 嵌入到 .rpp**（不是 SOURCE MIDI FILE 引用），格式 `E <delta_ppq> <status> <pitch> <vel>`
- **吉他不转 MIDI**：多声部转录至今未解决，保留音频
- **鼓 transcribe 用 librosa onset + 三段频谱分类**（kick<150Hz / hh>5kHz / 其它=snare），不依赖 ADTLib（ADTLib 安装坑多）
- **Demucs 调 CLI 子进程**，不用 Python API（API 跨版本不稳）
- **段落 marker 用 agglomerative segmentation**，命名是经验型（Verse/Chorus/Bridge），不保证准

### v0.2.0 — 历史索引 + 自动后处理 + LoRA wizard（2026-05-12）

commit 在 main 分支，本对话产出。

**三大功能**：
1. **生成历史索引器**（`lib/history.py` + `scripts/history-ui.py` :7861）
2. **自动后处理 watcher**（`scripts/auto-postprocess.py`）
3. **LoRA 数据准备 wizard**（`lib/training_data.py` + `scripts/lora-wizard.py` :7862）

**改动文件**：
- 新增 `lib/__init__.py`、`lib/history.py`、`lib/postprocess.py`、`lib/training_data.py`
- 新增 `scripts/history-ui.py`、`scripts/auto-postprocess.py`、`scripts/lora-wizard.py`
- 新增 `scripts/launch-history.sh`、`scripts/launch-lora-wizard.sh`、`scripts/launch-postprocess.sh`
- 修改 `requirements.txt`（加 watchdog/librosa/soundfile）
- 修改 `README.md`（重写，加完整工作流）
- 修改 `CHANGELOG.md`

**关键设计决策**（不要轻易改）：
- 三个 UI 独立端口（7860/7861/7862），不合并
- 历史索引用 UUID 而非文件名 hash，文件改名不丢追踪
- watcher 用文件后缀 + processed set 双重防循环
- LoRA wizard 是两阶段，分析与编辑分开

### v0.1.0 — 初始骨架（2026-05-12）

第一次 commit。

- `setup.sh` 一键安装
- `scripts/launch-ui.sh` 12GB VRAM 优化的 ACE-Step 启动（`--torch_compile --cpu_offload --overlapped_decode`）
- `scripts/normalize.sh` / `batch-normalize.sh` ffmpeg 归一化
- `prompts/styles.json` 15 个风格 tag 预设
- `prompts/lyrics-templates.md` 歌词结构模板

---

## 🚦 用户偏好（Communication Style）

- **直接给方案，不要长篇大论**。一上来先动手，结论放最后
- **被质疑能力时坦诚承认**比辩解好
- 喜欢**对比表格**和**进度清单**
- 测试时**只发结果截图**，让你解读
- 提的功能要**一次性全做完**，别漏
- 部署用 `git pull`，**不喜欢下载 zip 复制粘贴**
- **重要**：用户碰到下载链接挂掉的问题，**直接走 PAT git push 路线**，别推荐"换浏览器/手机/SSH"等方案，那都是绕路
- **本项目纯本地自用**，不要建议加 Web 后端、账号系统、付费功能、CDN、内容审核等
- 用户在另一个项目（truth-dare-wheel）也有类似对接记忆，**两个项目的偏好通用**

---

## ⚠️ 下一个 Claude 行动检查清单（用户明确要求）

**用户原话**（出自 truth-dare-wheel 记忆）：「下次我问你 有没有做个类似的 你先看看 识别本地代码遗留」

**意思**：当用户问"我们之前是不是做过 XX"或者"有没有类似的"时，**先在沙箱里查本地代码状态**，再回答。原因：

1. 沙箱里 `/home/claude/ai-music-lab/` 可能存有**之前没推到 GitHub 的半成品代码**
2. 也可能跟 GitHub 远端不同步，需要 `diff` 才看得出
3. 上来就说"我没做过 X" 是会犯大错的

**执行流程**：

```bash
# 1. 看本地有没有相关代码
grep -rn "<关键词>" /home/claude/ai-music-lab/ | head -20

# 2. 对比本地和远端
cd /tmp && rm -rf check-remote && git clone --depth 1 https://github.com/qiaodaxian233/ai-music-lab.git check-remote 2>&1 | tail -2
diff /tmp/check-remote/lib/<file>.py /home/claude/ai-music-lab/lib/<file>.py | head -40

# 3. 然后再回答用户
```

**别**只搜过往对话（`conversation_search`）就说"没做过" —— 那只能搜聊天记录，搜不到沙箱里的代码遗留。

---

## 🔮 还可以做的方向（用户问起再做）

按用户之前给出的 12 个功能列表，**已 12/12 全部完成** ✅。
v0.4.0 是 UI 整合,v0.5.0 一次性把剩余 8 项全做了。

### 已完成 ✅ (12/12)

- 1. 生成历史索引器（v0.2.0）
- 2. 自动后处理 hook（v0.2.0）
- 3. LoRA 训练数据准备 wizard（v0.2.0）
- 4. Prompt 预设 UI（v0.5.0 — Tab 📝 Prompt 工作室)
- 5. LoRA 管理面板（v0.5.0 — Tab 🎓 LoRA 库)
- 6. 批量生成（v0.5.0 — Tab 🚀 批量生成)
- 7. A/B 并排对比（v0.5.0 — Tab ⚖ A/B 对比)
- 8. Stem 分离（v0.3.0,作为 Song → DAW Export 的一部分;v0.5.1 加独立"🎼 一键 MIDI" Tab,跳过 stem 直接出 MIDI)
- 9. 自动封面图（v0.5.0 — Tab 🎨 封面 & 字幕,PIL 几何模式默认 / SDXL 可选)
- 10. 歌词字幕 LRC（v0.5.0 — Tab 🎨 封面 & 字幕,均分模式默认 / Whisper 可选)
- 11. 续写 / extend（v0.5.0 — Tab 🔁 续写 / 翻唱)
- 12. 翻唱 / audio2audio（v0.5.0 — Tab 🔁 续写 / 翻唱)
- ★ **统一控制台**(v0.4.0 起,v0.5.0 扩到 10 Tab,v0.5.1 扩到 11 Tab)

### ⚠ 下一个 Claude 可能要补的坑(v0.5.0 已知风险)

- **`lib/ace_step_api.py` 的参数签名是推断的**(基于 ACE-Step v1.5 主线)。如果用户跑 #6 批量生成 / #11 续写 / #12 翻唱时报 "unexpected keyword argument 'xxx'",去查 `ACE-Step-1.5/acestep/pipeline_ace_step.py` 实际 `__call__` 签名,改 `ace_step_api.py` 一处即可。**最可能出问题的参数**:
  - `manual_seeds` (有的版本叫 `seed`)
  - `save_path` (可能是 `output_path` 或 `output_dir`)
  - `task="extend"` / `"audio2audio"` (可能是别的字符串)
  - `repaint_start / repaint_end / ref_audio_strength` (这些都是用通用名,可能要换)
- **#5 多 LoRA 混合**: `lora_manager.format_mix()` 输出 `"path1:w1,path2:w2"`,具体 ACE-Step 是否支持这种字符串语法不确定。如果失败,看 ACE-Step LoRA loader 怎么写的,改 format_mix 输出格式。
- **#9 SDXL 模式 + ACE-Step 同时跑会 OOM**(12GB VRAM):做封面前先停 ACE-Step。geometric 模式无此问题。
- **#10 Whisper 模式精度依赖识别质量**: 中文用 `small` 起步, 英文 `tiny` 可能够。`make_lrc_whisper` 目前的"重对齐"策略是简单按比例匹配 segment,**不是真的强制对齐**。需要更高精度的话考虑 WhisperX 或 ctc-forced-aligner。

### 跟 v0.3.0 相关可能需要的后续改进

- **plugin chain 预设**：如果用户测试后觉得没 plugin 太裸，可以做"流行 plugin 模板"导入按钮（在 Reaper 里手动建一次 → 导出 RfxChain → 我把 RfxChain 路径写进 .rpp 引用）
- **MIDI 嵌入 fallback**：如果某些 Reaper 版本不认我嵌入的 MIDI events 格式，改成 `<SOURCE MIDIFLAC>` 或外部 .mid 文件引用
- **段落 marker 命名改进**：目前是经验型按 Intro/Verse/Chorus 顺序起，可以用 mfcc 相似度判断哪两段是"同一段重复"（chorus 通常重复）
- **吉他 MIDI**（这个是未解决问题,2026 年看是否有新论文）

### 其它可能要加的工程项

- `.github/workflows/` CI（lint Python、跑 unit test）
- `tests/` 单元测试（目前 `lib/` 没测试,只在沙箱里 smoke test 过)
- `configs/presets.yaml` 推理参数预设（不同场景不同 steps/guidance）
- 中文 ACE-Step LoRA 训练教程（用户最可能问的）

---

## 📝 此文件维护

下一次 Claude 做了重大改动，请：

1. 更新 CHANGELOG.md 加版本号
2. 在本文件「版本历史」加一条
3. 更新「项目结构」段反映新文件
4. 更新「lib/ 模块 API 清单」如果新增了函数
5. **推送本文件到 GitHub**（让记忆持续）

文件路径：`项目对接记忆.md`（仓库根目录）

---

*最后更新:v0.5.3(总控台 :7862 — 启停 + 监控 3 个服务 + GPU/磁盘/日志,新用户只双击 start-manager.bat 一个就能玩)*
*由 Claude (claude-opus-4-7) 与 qiaodaxian233 协作生成*
