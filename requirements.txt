# ai-music-lab 辅助工具的依赖
# (ACE-Step 自己的依赖由 setup.sh 通过 pip install -e . 装,gradio/numpy/torch 包含在内)

pyyaml>=6.0
mutagen>=1.47          # ID3 / metadata 读写 + 封面嵌入 (v0.5.0 #9)
pandas>=2.0            # CSV 处理
rich>=13.0             # CLI 输出美化

# 自动后处理 watcher
watchdog>=4.0

# LoRA wizard 音频分析(BPM/key 检测)
librosa>=0.10
soundfile>=0.12

# Song → DAW Export (v0.3.0)
demucs>=4.0            # 6-stem 分离 (htdemucs_6s, ~5GB 模型自动下载)
basic-pitch>=0.4       # Spotify audio-to-MIDI (钢琴/贝斯)
pretty_midi>=0.2       # MIDI 文件读写

# 封面图 (v0.5.0 #9) - geometric 模式必装
pillow>=10.0

# 拼音 (v0.5.2 #D) - 训中文 LoRA 必备
pypinyin>=0.49

# LoRA 权重融合 (v0.5.2 #B) - safetensors 读写
safetensors>=0.4

# 可选 (按需自己装,默认不装,装上才能用对应高精度模式):
# diffusers>=0.27 transformers>=4.40 accelerate>=0.30   # SDXL 封面 (#9 sdxl 模式, ~6GB 模型 + 6-8GB VRAM)
# openai-whisper>=20231117                              # LRC 强制对齐 (#10 whisper 模式, ~1-2GB 模型)
